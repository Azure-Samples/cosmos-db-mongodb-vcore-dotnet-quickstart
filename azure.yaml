# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: cosmos-db-mongodb-vcore-dotnet-quickstart
metadata:
  template: cosmos-db-mongodb-vcore-dotnet-quickstart
services:
  api:
    project: ./src/api
    language: dotnet
    host: containerapp
  web:
    project: ./src/web
    language: docker
    host: containerapp
pipeline:
  provider: github
